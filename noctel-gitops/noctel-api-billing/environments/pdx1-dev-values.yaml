global:
  cluster: pdx1
  namespace: dev
  environment: dev
  domain:
    prefix: pdx1-dev
    base: telnoc.com

noctel-api-billing:
  version: "latest"
  replicaCount: 3
  image:
    pullPolicy: Always
  env:
    SERVICE_NAME: billing
  secrets:

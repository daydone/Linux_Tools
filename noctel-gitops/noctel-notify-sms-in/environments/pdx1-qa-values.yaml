global:
  cluster: pdx1
  namespace: qa
  domain:
    prefix: pdx1-qa
    base: telnoc.com

noctel-notify-sms-in:
  version: "latest"
  replicaCount: 1
  image:
    pullPolicy: Always
  env:
    HEALTH_PORT: "9406"
    HTTP_PORT: "7406"
    SERVICE_NAME: noctel-notify-sms-in
  secrets:
    # TODO: add BANDWIDTH_WEBHOOK_USER
    # TODO: add BANDWIDTH_WEBHOOK_PASS
    # TODO: add SINCH_WEBHOOK_SECRET

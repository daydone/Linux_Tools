global:
  cluster: pdx1
  namespace: prod
  domain:
    prefix: pdx1-prod
    base: noctel.com

noctel-notify-webhook-in:
  version: "latest"
  replicaCount: 1
  image:
    pullPolicy: Always
  env:
    HEALTH_PORT: "9408"
    HTTP_PORT: "7408"
    SERVICE_NAME: noctel-notify-webhook-in
  secrets:

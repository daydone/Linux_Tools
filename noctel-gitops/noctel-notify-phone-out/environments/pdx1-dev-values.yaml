global:
  cluster: pdx1
  namespace: dev
  domain:
    prefix: pdx1-dev
    base: telnoc.com

noctel-notify-phone-out:
  version: "latest"
  replicaCount: 1
  image:
    pullPolicy: Always
  env:
    HEALTH_PORT: "9403"
    SERVICE_NAME: noctel-notify-phone-out
    SIP_API_URL: "http://asterisk.dev.svc.cluster.local:8088"
    SIP_API_TYPE: asterisk
  secrets:

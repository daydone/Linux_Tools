apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-qa-ingress
  namespace: qa
  annotations:
    traefik.ingress.kubernetes.io/router.tls.certresolver: dnsmadeeasy
spec:
  ingressClassName: traefik-qa
  rules:
  - host: pdx1-qa-api.telnoc.com
    http:
      paths:
      - backend:
          service:
            name: noctel-ape
            port:
              number: 3000
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - pdx1-qa-api.telnoc.com
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rancher-ingressroute
  namespace: cattle-system
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`pdx1-rancher.telnoc.com`)
      kind: Rule
      services:
        - name: rancher
          port: 80
  tls:
    certResolver: dnsmadeeasy
    domains:
      - main: pdx1-rancher.telnoc.com

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-lns-ingress
  namespace: prod
  annotations:
spec:
  ingressClassName: traefik-prod
  rules:
  - host: pdx1-prod-lns.telnoc.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: chirpstack-gateway-bridge
            port:
              number: 3001

  tls:
  - hosts:
    - pdx1-prod-lns.telnoc.com

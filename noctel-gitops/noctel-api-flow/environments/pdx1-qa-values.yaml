global:
  cluster: pdx1
  namespace: qa
  domain:
    prefix: pdx1-qa
    base: telnoc.com

noctel-api-flow:
  version: "latest"
  replicaCount: 3
  image:
    pullPolicy: Always
  env:
    PORT: "7240"
    HEALTH_PORT: "9240"
    SERVICE_NAME: flow
    CACHE_FILL_ENABLED: "true"
  secrets:

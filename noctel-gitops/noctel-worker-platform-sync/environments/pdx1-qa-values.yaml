global:
  namespace: qa
  imagePullSecrets:
    - name: harbor-pull
    - name: dockerhub-secret

noctel-worker-platform-sync:
  enabled: true
  extraEnv:
    - name: PLATFORM_SYNC_ENV
      value: "qa"

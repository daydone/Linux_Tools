global:
  namespace: dev
  imagePullSecrets:
    - name: harbor-pull
    - name: dockerhub-secret

noctel-worker-platform-sync:
  enabled: true
  extraEnv:
    - name: PLATFORM_SYNC_ENV
      value: "dev"

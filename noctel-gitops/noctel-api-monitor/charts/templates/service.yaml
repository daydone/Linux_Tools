apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.global.namespace | default "default" }}
  labels:
    app: {{ .Chart.Name }}
spec:
  type: ClusterIP
  ports:
  - port: 7260
    targetPort: http
    protocol: TCP
    name: http
  - port: 9260
    targetPort: health
    protocol: TCP
    name: health
  selector:
    app: {{ .Chart.Name }}

global:
  cluster: pdx1
  namespace: dev
  domain:
    prefix: pdx1-dev
    base: telnoc.com

noctel-api-realtime:
  version: "latest"
  replicaCount: 1
  image:
    pullPolicy: Always
  env:
    PORT: "7210"
    HEALTH_PORT: "9210"
    SERVICE_NAME: realtime
    ELASTICSEARCH_TIMEOUT_MS: "2000"
    CACHE_FILL_ENABLED: "true"
  secrets:

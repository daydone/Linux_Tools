global:
  cluster: pdx1
  namespace: qa
  domain:
    prefix: pdx1-qa
    base: telnoc.com

noctel-api-actions:
  version: "latest"
  replicaCount: 1
  image:
    pullPolicy: Always
  env:
    SERVICE_NAME: actions
  secrets:

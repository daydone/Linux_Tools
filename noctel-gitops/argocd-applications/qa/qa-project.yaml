apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: noctel-qa
  namespace: argocd
spec:
  description: NocTel QA Environment
  
  # Source repositories
  sourceRepos:
  - 'git@bitbucket.org:noctel-bitbucket/noctel-gitops.git'
  - 'https://charts.bitnami.com/bitnami'
  
  # Allowed destinations
  destinations:
  - namespace: qa
    server: https://kubernetes.default.svc
  
  # Allowed cluster resources
  clusterResourceWhitelist:
  - group: ''
    kind: Namespace
  - group: rbac.authorization.k8s.io
    kind: ClusterRole
  - group: rbac.authorization.k8s.io
    kind: ClusterRoleBinding
  - group: networking.k8s.io
    kind: IngressClass
  # SeaweedFS chart ships a CiliumEgressGatewayPolicy to pin filer
  # and s3 egress through a known node IP for upstream firewall
  # rules. CEGP is cluster-scoped per `kubectl api-resources
  # --api-group=cilium.io`, so it lives here even though it carries
  # a namespace field.
  - group: cilium.io
    kind: CiliumEgressGatewayPolicy
  
  # Namespace-scoped resources (allow all)
  namespaceResourceWhitelist:
  - group: '*'
    kind: '*'
  
  # Roles for this project
  roles:
  - name: qa-admin
    description: Full access to qa environment
    policies:
    - p, proj:noctel-qa:qa-admin, applications, *, noctel-qa/*, allow
    - p, proj:noctel-qa:qa-admin, repositories, *, *, allow
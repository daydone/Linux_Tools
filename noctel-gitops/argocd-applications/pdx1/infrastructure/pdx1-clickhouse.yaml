apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: clickhouse-pdx1
  namespace: argocd
  labels:
    cluster: pdx1
    component: clickhouse
spec:
  project: default
  source:
    repoURL: git@bitbucket.org:noctel-bitbucket/noctel-gitops.git
    targetRevision: HEAD
    path: clickhouse/charts
    helm:
      valueFiles:
        - ../environments/pdx1-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: clickhouse
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

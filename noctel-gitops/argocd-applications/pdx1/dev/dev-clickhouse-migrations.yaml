apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: noctel-clickhouse-migrations-dev
  namespace: argocd
  labels:
    environment: dev
    cluster: pdx1
    component: noctel-clickhouse-migrations
spec:
  project: noctel-dev
  source:
    repoURL: git@bitbucket.org:noctel-bitbucket/noctel-gitops.git
    targetRevision: HEAD
    path: noctel-clickhouse-migrations/charts
    helm:
      valueFiles:
        - ../environments/pdx1-dev-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
